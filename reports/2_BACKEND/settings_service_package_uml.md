# Settings Service Package Diagram

This diagram illustrates the high-level package structure of the Settings Service.

```mermaid
packageDiagram
    package "Settings Service" {
        [Spring Boot Application (src, pom.xml)]
        [Dockerfile]
    }
```