# User Service Package Diagram

This diagram illustrates the high-level package structure of the User Service.

```mermaid
packageDiagram
    package "User Service" {
        [Spring Boot Application (src, pom.xml)]
        [Dockerfile]
    }
```