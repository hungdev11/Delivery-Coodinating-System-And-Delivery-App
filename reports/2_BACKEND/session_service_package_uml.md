# Session Service Package Diagram

This diagram illustrates the high-level package structure of the Session Service.

```mermaid
packageDiagram
    package "Session Service" {
        [Spring Boot Application (src, pom.xml)]
        [Dockerfile]
    }
```