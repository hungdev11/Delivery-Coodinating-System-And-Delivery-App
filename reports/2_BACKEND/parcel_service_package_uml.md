# Parcel Service Package Diagram

This diagram illustrates the high-level package structure of the Parcel Service.

```mermaid
packageDiagram
    package "Parcel Service" {
        [Spring Boot Application (src, pom.xml)]
        [Dockerfile]
    }
```